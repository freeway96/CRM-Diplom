# Используем официальный образ Nginx (лёгкая версия на Alpine Linux)
FROM nginx:alpine

# Копируем все файлы проекта в стандартную директорию Nginx
# Точка означает "все файлы из текущей папки"
COPY . /usr/share/nginx/html

# Указываем, что контейнер будет использовать 80 порт
EXPOSE 8080

# Nginx запускается автоматически, команда не требуется